# Copy to .env and fill in your key. .env is gitignored - never commit the real one.

ANTHROPIC_API_KEY=

# Optional. Overrides the default model for generative pipeline stages.
# ORCHESTRATOR_MODEL=claude-sonnet-5
