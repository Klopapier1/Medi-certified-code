# Demo ProductSpec for the BMI pilot item.
# Fields match the Input table in the root README (orchestrator/config.py::ProductSpec).

NAME=bmi
REQUIREMENT="Calculate BMI from height and weight."
OUTPUT_DIR=products/bmi-demo
SAFETY_CLASS=A
